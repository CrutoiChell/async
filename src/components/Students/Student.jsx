export function Student({name,cost,amount}) {

    return (
        <div>
            <h2>Product page</h2>
            <p>Name: {name}</p>
            <p>Cost: {cost}</p>
            <p>Amount: {amount}</p>
        </div>
    )
}